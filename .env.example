# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files

DATABASE_URL="postgresql://postgres.[USERNAME]:[PASSWORD]@[HOSTNAME]:5432/postgres"

# ---------- Supabase ----------
NEXT_PUBLIC_SUPABASE_URL=https://hostname.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-key

# ---------- Site ----------
# Optional — defaults to http://localhost:3000
# On Vercel this is auto-set via VERCEL_PROJECT_PRODUCTION_URL if not provided.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"